export default class Place {
  constructor(id, title, imageUrl, address, lat, lng) {
    this.id = id;
    this.title = title;
    this.imageUrl = imageUrl;
    this.address = address;
    this.lat = lat;
    this.lng = lng;
  }
}
